package org.awong;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

/**
 * TODO figure out how to make Java & Kotlin interoperate
 */
public class MyLibraryTest
{
    @Test
    public void testMyLanguage() {
        assertEquals(1, 1);
        /*
        final var lib = new MyLibrary();
        assertEquals("Kotlin", lib.kotlinLanguage().name)
        assertEquals(10, lib.kotlinLanguage().hotness)
        assertEquals("OK", lib.start())
        */
    }

}
